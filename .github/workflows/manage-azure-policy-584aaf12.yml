name: manage-azure-policy-584aaf12
on:
  workflow_dispatch:
    inputs:
      remarks:
        description: 'Reason for triggering the workflow run'
        required: false
        default: 'Updating Azure Policies'


## Uncomment the following if you wish to automatically trigger workflow run with every commit
#
#  push:
#    paths:
#      - '3 - Core Policy Set/policies/Enforce_DenyAllVnetInBound_on_NSG_1f0ab688-0788-4f4a-989c-f8dd24e433f0/**'
#      - .github/workflows/manage-azure-policy-584aaf12.yml
jobs:
  apply-azure-policy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Login to Azure
      uses: azure/login@v1
      with:
        creds: ${{secrets.AZURE_CREDENTIALS_584aaf12}}
    - name: Create or Update Azure Policies
      uses: azure/manage-azure-policy@v0
      with:
        paths: |
          3 - Core Policy Set/policies/Enforce_DenyAllVnetInBound_on_NSG_1f0ab688-0788-4f4a-989c-f8dd24e433f0/**
